# ScienceRoomApp 引き継ぎメモ

## これは何か
『科学室アプリ_スマホ版仕様書.md』の MVP-1 実装。
学ぶ → 探索 → 素材発見 → 実験 → 成功/失敗 → 理解 → 技術解禁 のループが一周する状態。

## 構成
- Kotlin + Jetpack Compose（Material 3）、単一 Activity
- 画面遷移は `MainActivity.kt` の `AppRoot` が文字列ルートで管理（Navigation ライブラリ不使用）
- 保存は SharedPreferences に `PlayerState` を JSON で 1 レコード
- コンテンツは `app/src/main/assets/data/*.json`。コードに直接埋め込まない（仕様書の原則1）
- 画像は `app/src/main/res/drawable/` に 58 枚。`imageId` の文字列から `getIdentifier` で解決

## データ
| ファイル | 中身 |
|---|---|
| elements.json | 元素 30 |
| materials.json | 素材 40（自然 19 + 加工 21） |
| locations.json | 地域 8（森・川・山・海岸・洞窟・草原・湖・火山） |
| technologies.json | 技術 29（火起こし → 望遠鏡・灰吹法） |
| reactions.json | 反応 24 |
| temperature.json | 温度挙動 13 物質 |

## エンジン（`engine/Engine.kt`）
- `ExplorationEngine` … 探索回数と地域IDからシードを作る決定的乱択（仕様書の原則4）
- `ExperimentEngine` … 材料一致 → 温度/時間/器具の検証 → S/A/B/C/D 判定＋失敗原因の重み付け
- `LearningEngine` … 4モードの出題、忘却リスク順の優先出題、間隔反復（10分/1日/3日/7日/30日）
- `TechnologyEngine` … 前提技術・素材・知識・反応の充足判定と解禁処理
- `RecommendEngine` … 次の行動 TOP3 をルールで生成（原則2：判定はルール、AIは説明と推薦）

## 署名
`keystore/debug.keystore` をリポジトリに含め、debug / release とも固定の鍵で署名する。
これがないと GitHub Actions が実行ごとに違うデバッグ鍵を作るため、
新しい APK を入れるたびに「先にアンインストール」が必要になる。この鍵はデバッグ用で、
Play ストアに出す場合は別途リリース鍵を用意すること。

## ビルド
GitHub Actions（`.github/workflows/build.yml`）で `gradle assembleDebug`。
Actions の artifact から debug APK をダウンロードする。Gradle Wrapper は同梱せず、
`gradle/actions/setup-gradle` が Gradle 8.9 を用意する。

## MVP-2 で入れたもの（v2.0）
- `SerendipityEngine` … 定義外の組み合わせから確率 35%（シードは実験回数＋素材の並び）で研究候補を開放。
  `PlayerState.researchLeads` に記録し、実験室とプロフィールに一覧表示
- `HintEngine` … 実験成功率とクイズ正答率からヒント Lv を自動調整。手動固定も可（`autoHint`）
- `SkillEngine` … 仕様書28節の科学的思考力7項目（知識量/観察力/仮説形成/実験設計/結果解釈/原因分析/応用力）
- `RoadmapEngine` … 前提技術の深さから段を算出し、技術画面を段ごとの分岐表示に変更。
  各ノードに派生先（`└→`）を表示
- 図鑑に「化合物」タブ … 素材を単体 / 化合物 / 混合物に分類（`materials.json` の `kind`）。
  素材詳細に区分・採取か加工か・その素材を使う実験を追加

## MVP-3 で入れたもの（v3.0）
- 技術ツリーを 12 → 24 ノードに拡張（灰汁・漆喰・石積み・釉薬・磁器・ガラス器・鋳造・鍛冶・鋼・蒸留・顕微鏡・望遠鏡）。
  素材 32、反応 19。全 24 ノードが到達可能であることをスクリプトで検証済み
- `PlanEngine` … 目標技術から必要な知識・素材・実験・技術を逆算し（仕様書22節）、
  最短 / 学習重視 / 探索重視 / 実験重視の4ルートで手順を並べる（25節）。技術画面の「研究計画」から開く
- 目標設定（`PlayerState.currentGoal`）… ホームに目標と次の3手を表示
- `CivilizationEngine` … 完成した技術の系統（窯業 / 金属 / ガラス・光学 / 化学）から文明の傾向を判定してプロフィールに表示

## v3.5 で入れたもの
- 実験に分量を追加（`ExperimentInput.quantities`）。素材ごとに 1〜9 で指定し、所持数を超えると実行前に止まる
- `RatioEngine` … `reactions.json` の `ratios` と入れた量を最大公約数で正規化して比較。
  ずれると失敗原因に「材料の比」が追加される（ずれが小さければ部分成功のB判定）
- 比を設定した反応は11件（ガラス・銅・スズ・青銅・製鉄・たたら・消石灰・漆喰・釉薬・鋼・灰汁）
- 図鑑の反応タブに材料比を表示

## v5.3（v5.2 のビルド修正）
- `Engine.kt` に `GameMaterial` の import が抜けていた（`LinkEngine.materialsOf` の戻り値型）。
  これが解決できず、呼び出し側の `EncyclopediaScreen` でも型推論が連鎖的に失敗していた
- `TechScreen` で `filter` を使用より後ろで宣言していた
- 以後の確認手順：data / engine の公開型名を総当たりして import 漏れを検査し、
  `by remember` の宣言位置が使用より前にあるかも確認する

## v5.2 で入れたもの
- `LinkEngine` … 素材と元素の逆引き。図鑑の素材詳細に「つくり方 / 使い道 / 必要とする技術 /
  この先につながるもの」を、元素詳細に「この元素を含む素材 / この知識が要る技術」を表示。
  未発見の反応は名前を伏せたまま（"???"）関係だけ見せる
- 連鎖表示は3段・8件まで。木のように広がりすぎる素材があるため
- 実験手順のお気に入り（`PlayerState.favorites`、最大10件）。ノートから ☆ で登録し、
  実験室の上部からワンタップで条件を復元できる

## v5.1 で入れたもの
- 温度シミュレーターに5物質（亜鉛・鉛・銀・はんだ・ガラス）。
  はんだは共晶点183〜189℃で「単独よりも低い温度でとける」、ガラスは決まった融点を持たない、
  亜鉛は沸点907℃で製錬が難しい、という各物質の勘どころを見せる
- クイズに出題範囲（すべて / 第1〜3周期 / 金属 / 非金属と気体 / 覚えたものだけ）。
  30種から無差別に出すと散らばるため。選択肢も範囲内から作る
  ※ 金属判定は文字列 contains ではなく集合で行う。「非金属」も文字として「金属」を含むため
- 技術画面に絞り込み（すべて / 研究可能 / 着手中 / 完成）。29ノードで一覧が長くなったため

## v5.0 で入れたもの
- 合金と分離の系統を追加。鉱石3（閃亜鉛鉱・方鉛鉱・輝銀鉱）と金属3（亜鉛・鉛・銀）、
  合金2（真鍮・はんだ）で素材 40。反応5・技術5を足して反応 24・技術 29
- 灰吹法は「つくる」ではなく「混ざったものから目的の金属だけ取り出す」技術。蒸留と対になる題材
- 亜鉛は還元しても沸点907℃ですぐ蒸気になるため、冷やして集める点を解説に入れた
- 鉛は危険度4。害があることを解説に明記し、現実の手順は書かずシミュレーションとして扱う
- 文明ルートに「合金・分離」の系統を追加
- 全29技術に到達できることをスクリプトで再検証

## v4.9 で入れたもの
- 元素を 16 → 30 に拡張（He Li B F Ne P Ar Ti Cr Mn Ni Zn Ag Pb）。
  貴ガス3種が入って18族の柱が立ち、第2・第3周期がほぼ埋まった
- 周期表の色分けに「貴ガス」を追加。位置の重複がないことを検証済み
- 新元素は `locations` / `materials` が空。素材との紐付けは素材を足すときに行う
  （元素詳細では空なら行ごと出さない）

## v4.8 で入れたもの
- BGM（`Bgm.kt`）… 音源ファイルを持たず、AudioTrack にペンタトニックの分散和音を書き込んで鳴らす。
  既定はオフ。`onPause` で必ず止める。音源ファイルを使う形に変えるなら `res/raw` + MediaPlayer に差し替える
- 称号を10種から23種に拡充し、学習 / 探索 / 実験 / 技術 / 続ける の5分類と進捗バーを付けた。
  達成条件は `current` / `goal` の数値で持つので、未達のものも「あと何回か」が見える
- 図鑑の並べ替え。元素は原子番号 / 分類 / 未習得から、素材は名前 / 希少度 / 区分 / 所持数

## v4.7 で入れたもの
- 週替わりイベント（`EventEngine` / `GameEvent`）… サーバーを持たず、月曜始まりの週番号をシードに
  内容を組み立てる。同じ週なら誰でも同じ、週が変われば入れ替わる
- 3種類：探索の当たり週（対象地域は空振り率 -20、採取量 +1）／学習の週（対象分類を優先出題し正解の
  経験値2倍）／実験づくしの週（実験の経験値1.5倍）
- 対象地域は解禁済みの中から選ぶので、行けない地域が指定されることはない
- 週の目標（10〜20回）を達成すると経験値 +120。達成回数は `clearedEvents` に積み、称号「週の常連」に対応
- ホームに進捗バーと残り日数を表示。タップで対象の画面へ飛ぶ

## v4.6 で入れたもの
- 学習履歴（`DailyStat`、直近60日）… 日付が変わったとき、前日の活動量を差分で確定して積む。
  活動がなかった日は残さないので、久しぶりに開いても空の日で埋まらない
- 活動グラフ（`ActivityChart.kt`）… 直近14日をクイズ / 実験 / 探索の積み上げ棒で表示。
  グラフ用ライブラリは入れず Compose の Canvas だけで描いている
- 週次のふりかえり文（`MissionEngine.weeklyComment`）と、日ごとの記録7日分をプロフィールに表示

## v4.5 で入れたもの
- 周期表ビュー（`PeriodicTable.kt`）… 図鑑の元素タブで「一覧 / 周期表」を切り替える。
  18族×7周期のグリッドを横スクロール。分類ごとに色分けし、未習得は灰色で記号を伏せる。
  位置は `elements.json` の `group` / `period`（実際の周期表と一致することを検証済み）
- 元素の詳細に「第◯周期・◯族」を表示
- 素材タブに絞り込み。名前と組成（化学式）の両方で引ける

## v4.4 で入れたもの
- 初回起動チュートリアル（`TutorialScreen`、6ページ）。`PlayerState.tutorialDone` で1回だけ表示。
  プロフィールから再表示できる
- 効果音と振動（`Feedback`）… 音声ファイルを持たず端末の `ToneGenerator` で鳴らすので APK が太らない。
  探索の発見 / 実験の成否 / クイズの正誤 / 技術の解禁に割り当て。プロフィールで個別にオンオフ
- `MainActivity.onDestroy` で `ToneGenerator` を解放

## v4.3 で入れたもの
- クイズを10問1セッションに区切り、終わりに結果ダイアログ（正答率・最長連続・間違えた元素・講評）
- 復習モード（`nextQuestion(dueOnly = true)`）… 期限が来た元素が4個以上あるときだけ出題を絞る。
  ホームとクイズ冒頭から入れる
- 不正解のときは次回復習を3分後に短縮。正解なら従来どおり段階を進める
- ホームに「復習どきの元素が◯個」を表示し、タップでクイズへ

## v4.2 で入れたもの
- 復習リマインダー（`ReviewReminder` / `ReviewReceiver`）… AlarmManager の `setInexactRepeating` で
  1日1回。期限が来ている元素の数を数えて通知する。追加ライブラリなし、正確アラームの権限も不要
- 通知の許可は Android 13 以上でのみ実行時に求める。拒否されたら設定はオンにしない
- 端末再起動後の再予約はしていない。アプリを開いたときに `ReviewReminder.sync` で張り直す
- 今日のミッションを日付基準に作り直した（`DailyProgress`）。`Game` の init で日付が変わっていれば
  基準値を今の値に置き直す。進捗が「2 / 5」の形で出る

## v4.1 で入れたもの
- 固定のデバッグ用キーストアで署名（上の「署名」節）。以後は上書きインストールできる
- プロフィールに進行データの書き出し / 読み込み。JSON をクリップボード経由でやり取りする

## v4.0 で入れたもの
- 実験ノート（`PlayerState.notebook`、直近50件）… 素材・分量・温度・時間・器具・判定・原因候補・
  判定ルールの版（`ExperimentEngine.RULE_VERSION`）を残す。仕様書の原則4（再現可能）を画面から使える形にした
- 実験タブを3つに分割（実験室 / 温度 / ノート）。入力条件は `LabInput` に保持し、
  ノートの「この条件を読み込む」でそのまま復元して再実験できる
- ノートは成功・失敗で絞り込める。失敗の記録には原因候補が残るので、何を変えたかを追える
- `TitleEngine` … 称号10種。プロフィールに現在の称号と達成状況を表示

## 次にやること
- 未着手の画像：`tech_casting`（鋳造）と完成品4枚 `mat_tin` `mat_bronze` `mat_iron` `mat_steel`
- `tech_forging` と `tech_steel` の絵がほぼ同じ（どちらも金床＋ハンマー）。鋼は浸炭と焼き入れの構図にすると区別がつく
- `tech_plaster` は白い壁が主役の絵のため、1枚だけ不透過の四角で入っている
- AI 科学助手の連携は長期保留（Bonsai 側の受け入れが未整備）。着手するときは `BONSAI_API.md` を持ち込んでから
- クラウド同期とイベント
- `tech_salt` / `tech_lens` の画像差し替え、`elem_au` の刻印がクイズの答えを示す問題
